<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Crear Sheet"
    Height="650"
    Width="600">

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0"
               Grid.Column="0"
               Content="Colección:"
               HorizontalAlignment="Right"/>

        <ComboBox x:Name="S_Collection"
                  Grid.Row="0"
                  Grid.Column="1"/>

        <Label Grid.Row="1"
               Grid.Column="0"
               Content="Nombre hoja:"
               HorizontalAlignment="Right"/>

        <TextBox x:Name="S_sheet_name"
                 Grid.Row="1"
                 Grid.Column="1"/>

        <Label Grid.Row="2"
               Grid.Column="0"
               Content="Título plano:"
               HorizontalAlignment="Right"/>

        <TextBox x:Name="S_sheet_title"
                 Grid.Row="2"
                 Grid.Column="1"/>

        <Label Grid.Row="3"
               Grid.Column="0"
               Content="Formato:"
               HorizontalAlignment="Right"/>

        <ComboBox x:Name="S_Titleblock"
                  Grid.Row="3"
                  Grid.Column="1"/>

        <GroupBox Header="Selecciona vistas"
                  Grid.Row="4"
                  Grid.ColumnSpan="2"
                  Margin="0,10,0,10">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <ListBox x:Name="ViewsList"
                         Grid.Column="0"/>

                <ListBox x:Name="SelectedViews"
                         Grid.Column="1"/>
            </Grid>

        </GroupBox>

        <StackPanel Grid.Row="5"
                    Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">

            <Button x:Name="btnOK"
                    Width="80"
                    Margin="5"
                    Content="OK"/>

            <Button x:Name="btnCancel"
                    Width="80"
                    Margin="5"
                    Content="Cancel"/>

        </StackPanel>

    </Grid>

</Window>